version: '3.4'

services:
  rabbitmq:
    image: rabbitmq:3-management-alpine

  basket.data:
    image: redis:alpine

  basket.api:
    image: ${DOCKER_REGISTRY}basketapi
    build:
      context: .
      dockerfile: ../Basket.API/Dockerfile
    depends_on:
    - rabbitmq
    - basket.data
